apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "smartin-api.fullname" . }}-test-connection"
  labels:
    {{- include "smartin-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "smartin-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
