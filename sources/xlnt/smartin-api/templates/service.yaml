apiVersion: v1
kind: Service
metadata:
  name: {{ include "smartin-api.fullname" . }}
  labels:
    {{- include "smartin-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "smartin-api.selectorLabels" . | nindent 4 }}
