apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "notification-api.fullname" . }}-test-connection"
  labels:
    {{- include "notification-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "notification-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
