apiVersion: v1
kind: Service
metadata:
  name: {{ include "survey-api.fullname" . }}
  labels:
    {{- include "survey-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "survey-api.selectorLabels" . | nindent 4 }}
