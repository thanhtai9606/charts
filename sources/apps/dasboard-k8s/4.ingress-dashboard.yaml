# apiVersion: networking.k8s.io/v1
# kind: Ingress
# metadata:
#   name: dashboard
#   annotations:
#     nginx.ingress.kubernetes.io/backend-protocol: "HTTPS"
#     nginx.ingress.kubernetes.io/rewrite-target: /$2
#     nginx.ingress.kubernetes.io/configuration-snippet: |
#       rewrite ^(/dashboard)$ $1/ redirect;
#   namespace: kubernetes-dashboard
# spec:
#   rules:
#   - host: dashboard.becamex.com.vn
#     http:
#       paths:
#       - pathType: Prefix
#       - path: /dashboard(/|$)(.*)
#         backend:
#           service:
#             name: kubernetes-dashboard
#             port:
#               number: 443
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: dashboard
  namespace: kubernetes-dashboard
  annotations:
    nginx.ingress.kubernetes.io/backend-protocol: "HTTPS"
    nginx.ingress.kubernetes.io/rewrite-target: /$2
    nginx.ingress.kubernetes.io/configuration-snippet: |
      rewrite ^(/dashboard)$ $1/ redirect;
spec:
  rules:
  - host: dashboard.becamex.com.vn
    http:
      paths:
      - pathType: Prefix
        path: /dashboard(/|$)(.*)
        backend:
          service:
            name: kubernetes-dashboard
            port:
              number: 443
  tls:
  - hosts:
    - dashboard.becamex.com.vn
